[pytest]
asyncio_mode = auto
markers =
    unit: Fast tests with no external dependencies (DB, network, auth)
    integration: Tests with mocked DB and auth via FastAPI dependency overrides
testpaths = tests
